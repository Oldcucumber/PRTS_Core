# 本轮验证证据

MiniCPM 与 Qwen JSON 为完整 22 用例实测结果，包含原始模型响应和提示词；这些素材用于开发比较，不是独立盲测。记录中的 outputs 路径对应本机实验及 ZIP，Git 不附带完整媒体和权重。

swift-syntax.json 仅为 24 文件语法检查，不代表编译通过。extraction-checks.json 是独立解压四项检查摘要，不替代连续交互验收；912 漏提醒与 101 超时仍保留在修订报告。latency.json 从真实 20 路日志摘录，差值项没有伪装成独立阶段测量。
